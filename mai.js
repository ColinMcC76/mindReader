for(let i = 0; i < 100; i++) {
    var x = [];
    let random = "$%^#@!*)(!"
    if (i%9 == 0) {
        x[i] = "&";
    }
    else  x[i] = random.charAt(Math.floor(Math.random() * random.length)   
}